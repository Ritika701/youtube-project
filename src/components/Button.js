const Button = ({text}) => {
    return (
        <div>
            <button className='bg-gray-200 rounded-lg px-5 py-2 m-2 text-xs font-semibold'>{text}</button>
        </div>
    )
}

export default Button;